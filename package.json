{
  "name": "neil-tanamor-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio website for Neil Ivan V. Tanamor.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev:worker": "node scripts/build.js && npx --yes wrangler dev --port 8787",
    "start": "node server.js"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "nodemailer": "^9.0.5"
  }
}
